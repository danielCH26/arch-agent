# =============================================================================
# requirements.txt — Dependencias Python del Asistente de Arquitectura
# Stack: Chainlit (UI) + LangChain (agente) + PGVector (RAG) + Langfuse (tracing)
# =============================================================================

# --- UI ---
chainlit==1.3.5

# --- LLM / Agente ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0

# --- RAG: Embeddings ---
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# --- RAG: PGVector ---
langchain-postgres>=0.0.12
psycopg[binary]>=3.2.0
pgvector>=0.3.6

# --- ORM ---
sqlalchemy>=2.0.0

# --- Observabilidad (Langfuse) ---
langfuse>=3.0.0
# Langfuse expone su handler para LangChain via:
# from langfuse.langchain import CallbackHandler

# --- Config / Settings ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# --- HTTP / utilitarios ---
httpx>=0.27.0
tenacity>=8.3.0

# --- MCPs (Python) ---
mcp>=1.0.0
