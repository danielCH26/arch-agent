# =============================================================================
# requirements.txt - Asistente de Arquitectura (F01: smoke test del entorno)
# =============================================================================
# Mantenlo MINIMO. Cada feature nueva (F02..F23) agrega sus deps aca.
# =============================================================================

# --- UI (lo unico que necesitamos para F01) ---
# chainlit 2.x: compatible con pydantic 2.10+ (a diferencia de 1.3.x)
chainlit==2.0.6
# Deps transitivas de chainlit 2.x que no se instalan solas
sniffio>=1.3.0
asyncer>=0.0.7
literalai>=0.0.600

# --- Driver async para Chainlit data layer (requerido por chainlit>=1.0) ---
asyncpg>=0.29.0

# --- Config / Settings ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# =============================================================================
# F02+ (DESCOMENTAR progresivamente cuando se implemente cada feature)
# =============================================================================

# F02 - Arquitectura tecnica / configuracion general
# langchain>=0.3.0
# langchain-core>=0.3.0
# langchain-openai>=0.2.0
# langchain-community>=0.3.0
# langgraph>=0.2.0
# sqlalchemy>=2.0.0

# F06 - Pipeline RAG (PGVector + embeddings)
# langchain-postgres>=0.0.12
# psycopg[binary]>=3.2.0
# pgvector>=0.3.6
# langchain-huggingface>=0.1.0
# sentence-transformers>=3.0.0
# torch>=2.0.0
# NOTA: torch + transformers = ~1.5GB. Instalar solo cuando se active RAG.

# F14 - Logging / observabilidad
# langfuse>=3.0.0
# opentelemetry-api>=1.27.0
# opentelemetry-sdk>=1.27.0

# MCPs (cliente Python)
# mcp>=1.0.0
